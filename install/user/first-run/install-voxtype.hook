#!/bin/bash

set -e

if omisu-done ensure voxtype-install-invitation; then
  omisu-notification-send -u critical -g  "Install Dictation with Voxtype" \
    "Click to install voice dictation for OmiSu." \
    --exec omisu-launch-floating-terminal-with-presentation omisu-voxtype-install
fi
