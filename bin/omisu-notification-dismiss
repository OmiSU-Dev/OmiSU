#!/bin/bash

# omisu:summary=Dismiss a notification by summary substring. Used by the first-run notifications to dismiss them after clicking for action.
# omisu:args=<summary>

if (($# == 0)); then
  echo "Usage: omisu-notification-dismiss <summary>"
  exit 1
fi

omisu-shell -q notifications dismiss "$1"
