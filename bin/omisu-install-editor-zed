#!/bin/bash

# omisu:summary=Install Zed Editor and configure it with the current OmiSu theme

echo "Installing Zed Editor..."
omisu-pkg-add zed omazed

# Apply OmiSu theme to Zed
omazed setup

setsid uwsm-app -- gtk-launch dev.zed.Zed >/dev/null 2>&1 &
