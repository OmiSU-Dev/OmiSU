#!/bin/bash

# omisu:summary=Toggle suspend availability in the system menu

omisu-toggle suspend-off

if omisu-toggle-enabled suspend-off; then
  omisu-notification-send -g 󰒲 "Suspend removed from system menu"
else
  omisu-notification-send -g 󰒲 "Suspend now available in system menu"
fi
