#!/bin/bash

# omisu:summary=Update user-installed git themes

mapfile -t themes < <(omisu-theme-extras)

for theme in "${themes[@]}"; do
  echo "Updating: $(basename "$theme")"
  git -C "$theme" pull
done
