#!/bin/bash

# omisu:summary=Show current theme
# omisu:examples=omisu theme current

THEME_NAME_PATH="$HOME/.local/state/omisu/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g' "$THEME_NAME_PATH"
else
  echo "Unknown"
fi
