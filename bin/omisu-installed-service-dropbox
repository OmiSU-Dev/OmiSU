#!/bin/bash

# omisu:summary=Check whether Dropbox is installed and running
# omisu:hidden=true

set -euo pipefail

if omisu-cmd-present dropbox-cli && dropbox-cli running >/dev/null 2>&1; then
  exit 0
fi

pgrep -x dropbox >/dev/null 2>&1
