#!/bin/bash

# omisu:summary=Launch 1Password or start its installer when missing.

set -e

if omisu-cmd-present 1password; then
  exec setsid uwsm-app -- 1password
else
  exec omisu-launch-floating-terminal-with-presentation omisu-install-service-1password
fi
