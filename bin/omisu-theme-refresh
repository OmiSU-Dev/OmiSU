#!/bin/bash

# omisu:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.local/state/omisu/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  OMISU_THEME_SKIP_BACKGROUND=1 omisu-theme-set "$(cat $THEME_NAME_PATH)"
fi
